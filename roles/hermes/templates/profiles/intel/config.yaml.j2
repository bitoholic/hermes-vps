tools:
  terminal:
    enabled: false
  filesystem:
    enabled: false
  git:
    enabled: false

model:
  provider: "openrouter"
  default: "openrouter/free"

web:
  backend: "nous"
  use_gateway: true
