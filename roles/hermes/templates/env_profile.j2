OPENROUTER_API_KEY="{{ secrets.profiles[item.name].openrouter_api_key }}"
NOUS_API_KEY="{{ secrets.profiles[item.name].nous_portal_api_key }}"
CONTEXT7_API_KEY="{{ secrets.profiles[item.name].context7_api_key | default('', true) }}"
GITHUB_TOKEN="{{ secrets.hermes_default_github_token | default('', true) }}"
GIT_AUTHOR_NAME="{{ secrets.git_username | default('', true) }}"
GIT_AUTHOR_EMAIL="{{ secrets.git_email | default('', true) }}"
GIT_COMMITTER_NAME="{{ secrets.git_username | default('', true) }}"
GIT_COMMITTER_EMAIL="{{ secrets.git_email | default('', true) }}"
HERMES_WRITE_SAFE_ROOT="/opt/data:/tmp"
